org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true

javaVersion=21
neoForgeVersion=21.1.219
minecraftVersion=1.21.1

# TerraFirmaCraft — needed at compile time by our QuernBlockEntity Mixin.
# Local builds prefer a jar in libs/ (gitignored); CI falls back to the Modrinth Maven.
# This value is the Modrinth version-ID (NOT the human version). Find it at
# https://modrinth.com/mod/terrafirmacraft/versions  (HjUho0IR = 4.1.3, matches the pack)
tfc_maven_version=HjUho0IR

mod_version=1.0.0
mod_id=tfprevived_compat
mod_name=TerraFirmaPunk Revived Compat
mod_author=Mr.101Freaking
mod_license=MIT
